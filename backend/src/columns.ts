/** Default column titles for new boards (in order). */
export const DEFAULT_COLUMN_TITLES = [
  "Backlog",
  "High priority",
  "ToDo",
  "In Progress",
  "Ready For Acceptance",
  "Done",
] as const;
