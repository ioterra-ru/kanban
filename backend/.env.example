PORT=4000
DATABASE_URL="postgresql://kanban:kanban@localhost:5432/kanban?schema=public"
CORS_ORIGIN="http://localhost:5173"
SESSION_SECRET="change-me-change-me-change-me"
PUBLIC_BASE_URL="https://localhost:8443"

# Optional: email notifications (SMTP). If SMTP_HOST is not set, emails are disabled (events will be logged).
#SMTP_HOST="smtp.example.com"
#SMTP_PORT=587
#SMTP_SECURE=false
#SMTP_USER="user@example.com"
#SMTP_PASS="password"
#SMTP_FROM="IoTerra Kanban <no-reply@example.com>"

