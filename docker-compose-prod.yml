# Все параметры (порты, хосты, URL и т.д.) задаются через env-файлы в docker/compose/:
#   --env-file docker/compose/.cont_one_app.env
#   --env-file docker/compose/.cont_one_app.secrets.env
services:
  db:
    restart: always
  backend:
    restart: always
  frontend:
    restart: always

networks:
  default:
    name: kanban_default

volumes:
  kanban_pg:
  kanban_uploads:
